dotnet new mvc - cria um projeto no molde MVC
tipo o new console


dotnet dev-certs https --trust - comando para gerar certificado de segurança https sempre fazer antes de rodar,
só é necessario uma vez no computador

dotnet ef migrations add InitialCreate - cria a migrations
"ef" de entity framework

BOOTSTRAP CSS = FRAMEWORK
